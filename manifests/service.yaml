kind: Service
metadata:
  name: solar-system
  labels:
    run: solar-system
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: solar-system

